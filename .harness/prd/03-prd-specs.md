<!-- SUMMARY: N-Agent 的迭代需求池，未经人工确认、禁止AI阅读或修改 -->
# 产品需求 - 迭代演进

## 约束
本文仅供自然人使用，未经人工确认、禁止AI阅读或修改。

## 需求列表
编码要求：严格遵循DDD分层规范。文档要求：①说人话，②极简

[20260801]
- FR
    - 前端：按钮，调整项目中的所有按钮FE样式，使对齐`对话-会话-新增`按钮的样式
    - 对话：Dashboard Chat框，role=user且非用户发送的消息，从Chat框左侧、改到右侧(其它样式保持不变)
    - 对话：Dashboard Chat框，role=user且非用户发送的消息，消息气泡水平最大宽度、跟用户发送的消息保持一致
    - 对话：工具操作确认，时间渲染改用项目规范要求的UTC+8时区；同时查漏补缺，看看其它地方是否有不遵循UTC+8的情况，也修复了
    - 任务：管理-任务详情，底部的按钮(如删除、重试等)移动到Panel Header的右上角
    - 工具：Browser自动化，E2E验收和迭代
        - 容器：接管释放后，交给Agent自动化操作后，实时视图没有更新、刷新一次Dashboard后才能恢复自动更新
        - Host：Host CDP，接管释放后，实时视图显示`截图暂不可用`；Chat框交互一轮后，实时视图恢复可用，但显示元素引用过期失败（container貌似遇到过类似问题）
        - 浏览器：视图页面布局调整，实时视图、控制与历史调整为水平两列，宽度分别80%、20%，设置最小高度占满屏幕；操作历史从表格改为合适的展示形式(只有20%水平宽度、表格不再合适)
        - 浏览器：控制与历史，干掉右上角刷新按钮，改为根据浏览器任务状态自动驱动，只有状态更新时才刷新、且容器内局部刷新避免闪屏。可以复用实时视图的定时器
        - 浏览器：实时视图的状态(如已关闭)、后端(如本机)，放到标题`实时视图`后，形如`实时视图(已关闭|本机)`
        - 浏览器：用户对话要求截图后，支持在对话框中展示图片(结果：在工具卡片展示，会话结束后会消失)

[20260802]
- NFR
    - 文档：Browser Use，容器/本地CDP 工作原理
- FR
    - 制品：[KF]Artifact 制品工作台，将附件和 Artifact 升级为可预览、编辑、导出和发布的结果交付体系。产品功能参考Hermes、Manus、Workbuddy

[20260803]
- FR
    - 制品：Artifact 制品工作台，验收和迭代
        - 左导，定时任务 调整到 任务上方
        - 制品详情Panel，原始文件(Origin)下拉按钮的FE样式非常差，导出按钮干掉下拉、只支持导出原始文件(Origin)格式
        - 制品详情Panel，按钮字号调整为项目标准，补齐.btn字号13px默认配置
        - 制品详情Panel，任务附件 链接地址改为任务详情页(带taskid)，改名为任务、放到产出物名称后面形如`产出物名称(任务)`，链接只放到任务两个字上、不放到括号上
        - 制品详情Panel，属性信息(如`大小: 28 B · 更新: 2026-08-03 08:31:52`)左对齐下方文本区，Padding Top 5px，字号改为13px
        - 制品详情，要能被直接链接访问，访问来源可以是 任务型对话、任务详情页
        - 制品详情，在编辑按钮右侧、新增删除按钮，支持删除制品实例
        - 制品，筛选条件来源、类型、状态放到一行
        - 制品，搜索名称、筛选条件的字号调小到13px
        - 任务详情页，增加制品详情链接
        - 对话框，给产出制品消息、增加详情链接
    - 对话：右侧边栏在`调试信息`之外再增加制品信息，调试信息改名为工具调用，新增的制品信息复用`制品`页左侧Pannel内容、弹出样式跟`调试信息`保持一致
        - 右侧边栏，展开/收起控制，从右侧边自定义按钮、改为对话框右上角的`展开/收起`按钮(使用分栏切换图标)，样式可参考workbuddy
        - 右侧边栏，高度跟对话框对齐；右侧边距，可以干掉；`展开/收起`按钮，视作右侧边栏的一部分，展开状态时按钮在右侧边栏的右上角
        - 会话，干掉`新增`按钮；会话、对话框、右侧边栏，Pannel Head高度调整为一致
        - 对话：右侧边栏，多Tab切换时、被选中Tab去掉蓝色下划线
        - 对话：会话、对话框、右侧边栏，三区底色调整、使逐渐变浅，右侧边栏底色维持现状，会话要调深一点
        - 对话：右侧边栏，干掉顶部文字`更多信息`，但要保留独立Head、保留右上角的`展开/收起`按钮
        - 对话：对话框，去除输入框本身的底色
        - 对话：会话、对话框、右侧边栏，距离底部Gap太大了，调小到10px

[20260804]
- FR
    - 对话：集成优化
        - 对话：对话页面，禁止垂直方向滚动
        - 对话：右侧边栏未展开时，对话框右侧Padding是多少？右侧边栏展开时，右侧边栏右侧Padding也要这个取值
        - 对话：对话关联制品，展示不对，会话task-d1d97cd7-36b8-5001-8c24-fb62bee639da，任务e2e-art-1785716605-86486-task

[20260808]
- FR
    - 制品：Artifact 制品工作台，验收和迭代
        - 任务：默认授予 execute_code 权限
        - 会话 dashboard-31fea229-1bec-4989-9146-8596d2cbd21f，出现卡片 `查询状态: work task t_b50f753f89204439`，这个卡片标题不对，期望是`任务状态`
        - 会话 dashboard-53f61ffa-21b2-4cbc-b625-f348197bb8da，①制品信息未正确关联 ② task-output-a.txt、 task-output-b.md被错误标记为`其它`类型 从而无法渲染
        - 会话 dashboard-53f61ffa-21b2-4cbc-b625-f348197bb8da，制品气泡 `产出制品: task-output-a.txt 详情`、`产出制品: task-output-b.md 详情` 合并到气泡 `任务已完成：测试：任务制品 xxx`中
        - 对话：工具调用、制品信息，需要手动刷新浏览器、才能加载最新，要求自动加载最新
        - 任务：任务详情页，附件的上传按钮，风格不符合项目规范
        - 制品：已删除的任务，其制品还展示在列表，如e2e-art-1785716569-85752-att2.txt，期望不展示
        - 制品：任务t_d214a4e52c64494e，附件`横向-邮箱归属.md`，任务详情页没有制品链接、附件也没有展示在制品列表，文件名不支持Unicode？
        - 制品：预览内容，最大高度应达到预览Pannel的底部，现状占据了50%左右
        - 制品：制品列表、预览两个Pannel，最大高度不超过屏幕底部，最好Padding Bottum 10px；制品列表超出高度后，支持垂直方向滚动
        - 任务：管理-任务看板，底部Padding 也向对话框页对齐
        - 制品：附件从制品页删除后，任务附件没有同步删除，这种问题太Low了

[20260809]
- FR
    - 对话：产出物，如果无法以 Chat 消息呈现，则必须以标准制品呈现
    - 制品：Artifact 制品工作台，验收和迭代
        - 验收：1.4 手动制品：对于1.3已创建的任务，手动创建一个 Artifact，工作台列表恰显示五个制品。手动创建Artifact没有必要
        - 制品：预览，py、json，最大高度未达到预览Pannel的底部(这还能异构?!)；pdf，无法预览
        - 制品：预览，编辑状态下、`编辑框+保存按钮`最大高度未达到预览Pannel的底部，修改所有附件类型
        - 制品：导出，使用制品名称、作为默认的文件名，当前恒为export
        - 制品：发布，制品发布状态展示在头部状态栏，形如 `大小: 3703 B · 更新: 2026-08-09 15:59:27；已发布: [链接](link)`，同时`发布`按钮更名为`撤回`；编辑并保存、形成新制品后，发布状态恢复为默认的未发布，同时撤销已发布的老快照
        - 制品：发布，删除源 Artifact 后，不但同步清理发布快照、公开链接失效不再返回制品内容，还要把数据库 published_artifacts.artifact_id 变为 NULL，快照记录与文件也同步删除
        - 制品：预览，bill-qwen-26-4.csv的列太紧凑了、查阅不方便
    - 治理：修正测试用例，使全量通过
    - 概览：左导干掉`概览`菜单，保留域名默认页(仍使用`概览`页面)、概览路由
    - 概览：功能入口，补全制品、浏览器等缺失项，调整功能左右排序、使跟左导菜单上下顺序一致
    - 文档：制品，介绍生成原理
 
[20260810]
- FR
    - 制品：支持对话原生制品，包括普通Chat生成和修改制品、制品支持Revision版本，导出能力DOCX/PPTX/XLSX
        - 导出：FE样式优化，导出使用项目标准按钮，点击后跳出modal(项目标准样式)、让用户确认额外信息(如格式)。同时，新增FE按钮/菜单规范，除左导、顶导外，禁用pop二级菜单(如修改前的导出)、除非用户明确要求
        - 导出：优化modal内容布局，Sector 1命名为格式，取值内容为单选、每行2个取值
        - 导出：制品attachment-b.md，选择docx格式，导出文件仍以.md后缀、应该是.docx；html、pptx我没测试，你一并检查下导出文件后缀
        - 版本：版本历史，触发方式为右上角`版本`按钮、放到发布右侧，形式改用项目标准弹出框modal，modal内容样式要对人友好

[20260811]
- HE
    - 文档：根据代码现状，更新DDD文档`## Artifact`章节。文档要求 ①说人话、②极简
- FR
    - 制品：支持对话原生制品，验收和迭代
        - 任务：通过命令或Chat创建task失败，修复之
        - 任务：任务完成后，无法通过自然语言、修改制品，不符合设计预期、修复之
        - 制品：编辑后，预览不自动更新、手动刷新浏览器才更新，版本也有类似不自动刷新的问题
        - 制品：发布/撤销跟随版本，对应的操作按钮也移到版本modal
        - 制品：预览，状态栏增加版本信息，格式如：`大小: 17 B，更新: 2026-08-11 14:26:08；版本: v1，已发布: 链接`

[20260812]
- HE
    - HE：新增人工E2E验收文档
        - 提示词：根据.harness/harness-third-review-codex.diff，将.harness中的 `verify人工E2E验收` 相关的所有变更、更新到harness模板项目：/Users/niean/code/github.com/niean/harness-tpl。注意：①不要遗漏 ②不要连带其它diff(如after hook、third review等)
    - HE：项目.harness中的Third Review，耦合Codex、不通用。评估下，是否可抽象为third-review Hook(就像after-finish Hook一样)
    - HE：重构，Third Review放到通用框架，Hooks移到项目私有
- FR
    - 调度：[KF]支持多Agent委派与并行，入口无关(不限于Task)

[20260823]
- HE
    - HE：harness，修复Skill三方审阅
    - HE：三方审阅spec/plan，相比08.11的版本、08.24的执行耗时增加了非常多，分析原因并优化
    - HE：locals/harness_tmp/run-third-review-background.sh 这是一次性的临时文件，还是harness 三方审阅的正式脚本？
    - HE：三方审阅spec/plan，降低要求，审阅期间仓库 HEAD、Index及目标外文件发生变化时抛出警告(继续执行)而非错误(阻塞执行)
- FR
    - 调度：支持多Agent委派与并行，验收和迭代
        - 会话：delegation-cf976103-e1cd-4250-9947-5cf5d5aa313d，系委派子Agent产生的隔离会话，没有被正确命名、跟既有的会话记录风格不一致
        - 对话：delegation委派会话，默认不应该出现在`对话`页的`会话`列表中，除非用户刻意筛选；也就是说，`对话`页的`会话`列表要支持类型设置、筛选会话类型，使用项目表准的modal
            - 会话类型，从按钮改为筛选图标，图标大小跟本页其它图标保持一致；modal框中的checkbox，改为4个/行，check图标和文字放到同一行
            - 会话：会话列表，也增加筛选图标、modal，功能和样式跟对话页的会话筛选modal保持一致
        - 会话：会话列表，delegation类型的会话详情，消息数量为0、是否符合预期？符合预期，delegation不做持久化
        - 会话：父会话删除后，delegation委派会话应该同步删除，避免出现野delegation会话
        - 会话：会话删除时，同步删除关联的任务、以及任务关联的制品，等
    - 对话：会话Sector，支持左侧隐藏/展开操作，使用按钮图标，隐藏后对话框同步扩展，同时要考虑好右边栏展开的情况
        - 收起状态时，图标也要同步修改；右侧边栏展开时，左侧边栏展开/收缩时、右侧边栏水平宽度保持不变
        - 对话框右侧Padding取值，在右侧边栏收起/展开时不一致
    - 对话：会话Sector，支持搜索、使用图标按钮，放在筛选按钮左侧；点击搜索图标后，在modal框支持按会话名称搜索。产品形态/FE样式 参考workbuddy

[20260824]
- HE
    - HE：三方审阅，移除全仓库保护，包括全仓快照、HEAD/Index/未跟踪文件检查与目标实时监控，只保留spec/plan目标文件管理
    - 文档：多Agent委派
- FR
    - 前端：左导按钮位置调整，`任务`移到`会话`下方，`制品`移到`执行器`下方
    - 对话：FE样式优化
        - 会话Sector，搜索modal使用固定高度、不要动态调整高度
        - 会话Sector，展开状态时、图标按钮的左侧底色要深一点，大概率有现成的图标可用
        - 对话框，左右侧边栏展开/收缩时，输入框、对话内容的水平宽度要类似workbuddy的自适应变化，而不是占满水平最大宽度或固定宽度
    - 对话：输入框支持设置技能。参考豆包工作任务，给出对话框-输入框增加`技能`设置，放到`记忆`之后。`技能`设置采用类似`记忆`的弹出框，开视觉伴侣来确认
        - Skills：Skills列表，新增设置项、控制Skill是否出现在对话框-输入框的`技能`设置中；默认可见，可通过`编辑`modal来修改

---

[待办]
- HE
- NFR
    - 知识：UDS，详细原理、Go样例
    - 集成：N-Agent套装，集成N-KB；发布镜像，完成一键冷启动
    - 前端：使用Element UI，重构前端代码，要求①保持功能一致、②最大限度的使用Element UI组件库(减少自己写的代码)。Element UI的项目规范，参考 /Users/niean/code/git.zuoyebang.cc/odin/odin-fe
    - 任务：LLM请求未命中缓存读，分析原因
- FR
    - 任务：持久化执行环境，让任务拥有快照、可恢复(如执行`sh docker/restart.sh`后自动恢复)
    - 沙盒：远程沙盒，包括云沙盒、私有VM沙盒
    - 工具：Computer Use(MacOS)
    - 管理：接入配置，秘钥Store(类似平台)
    - 产品：对标Manus，连接器、Project、Tenant(低优)
    - 产品：对标DSH，发现值得参考的技术架构、产品功能
    - 调度：支持MoA

---

[待验证]
- 上下文
    - 消息时序，各角色是否OK
    - 工具描述，字节是否稳定
    - 缓存命中
- 工具
    - MCP支持stdio类型

